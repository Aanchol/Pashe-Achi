import 'package:flutter/material.dart';

void main() {
  runApp(const PasheAchiApp());
}
class PasheAchiApp extends StatelessWidget {
  const PasheAchiApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
